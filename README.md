1. this project needs mysql and mysql.connector installed n running
2. substitute your values in the following line.
  db=mysql.connector.connect(user=" ",passwd=" ",host=" ")

usually user="root" and host="localhost"
